# Shared Frontend Features

Shared feature-level UI and helpers live here when they are used by more than one mode.

Prefer mode-specific components under `pages/customer`, `pages/totem`, or `pages/admin` until reuse is clear.
